{
  "kind": "collectionType",
  "collectionName": "cookie_categories",
  "info": {
    "singularName": "cookie-category",
    "pluralName": "cookie-categories",
    "displayName": "Cookie Category",
    "description": "Cookie consent categories - necessary, analytics, marketing, etc."
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {
    "i18n": {
      "localized": true
    }
  },
  "attributes": {
    "name": {
      "type": "string",
      "required": true,
      "description": "Category name (e.g. Necessary, Analytics)"
    },
    "slug": {
      "type": "uid",
      "targetField": "name"
    },
    "description": {
      "type": "text",
      "description": "What this cookie category is for"
    },
    "sortOrder": {
      "type": "integer",
      "default": 0,
      "description": "Display order in the consent popup"
    },
    "isActive": {
      "type": "boolean",
      "default": true,
      "description": "Show or hide this category"
    },
    "isRequired": {
      "type": "boolean",
      "default": false,
      "description": "If true, user cannot disable (e.g. essential cookies)"
    },
    "isDefaultEnabled": {
      "type": "boolean",
      "default": false,
      "description": "If true, toggled on by default"
    },
    "cookies": {
      "type": "json",
      "description": "Array of cookies in this category"
    },
    "privacyPolicyUrl": {
      "type": "string",
      "description": "Link to more info about this category"
    }
  }
}
