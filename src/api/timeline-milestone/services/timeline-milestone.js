'use strict';

/**
 * timeline-milestone service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::timeline-milestone.timeline-milestone');
