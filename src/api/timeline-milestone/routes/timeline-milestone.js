'use strict';

/**
 * timeline-milestone router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::timeline-milestone.timeline-milestone');
