{
  "collectionName": "components_free_trial_trust_item",
  "info": {
    "displayName": "Trust Item",
    "description": "Trust signal for Free Trial page (e.g. No credit card, Free trial)"
  },
  "attributes": {
    "label": {
      "type": "string",
      "required": true,
      "description": "Trust item text (e.g. No credit card required)"
    },
    "icon": {
      "type": "string",
      "description": "Font Awesome icon class (e.g. fa-shield-alt, fa-check-circle)"
    },
    "sort_order": {
      "type": "integer",
      "default": 0,
      "description": "Display order (lower number appears first)"
    },
    "is_active": {
      "type": "boolean",
      "default": true,
      "description": "Show or hide this trust item"
    }
  }
}
